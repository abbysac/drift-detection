<<<<<<< HEAD
# name: Guaranteed OIDC Test

# on:
#   push:
#     branches: [ "main" ]

# permissions:
#   id-token: write
#   contents: read

# jobs:
#   debug:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Show GitHub Context
#         run: |
#           echo "Repository=${{ github.repository }}"
#           echo "Repository Owner=${{ github.repository_owner }}"
#           echo "Ref=${{ github.ref }}"
#           echo "Ref Name=${{ github.ref_name }}"
#           echo "Event=${{ github.event_name }}"
#           echo "Actor=${{ github.actor }}"

#       - name: Get OIDC Token
#         run: |
#           TOKEN=$(curl -s \
#             -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
#             "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=sts.amazonaws.com" \
#             | jq -r '.value')

#           echo "Token received: ${#TOKEN} characters"

=======
>>>>>>> 4560fc03478a7107d9e4182dfe37cdc15cde97cc
name: Terraform Drift Detection

on:
  push:
    branches: [ "main" ]
  schedule:
    - cron: "0 2 * * *"  # daily at 2 AM UTC
  workflow_dispatch: {}

permissions:
  id-token: write
  contents: read

jobs:
  drift:
    runs-on: ubuntu-latest
    defaults:
      run:
<<<<<<< HEAD
        working-directory: ./terraform   # <-- adjust to your actual path
=======
        working-directory: ./terraform   # <-- adjust to your actual path #######
>>>>>>> 4560fc03478a7107d9e4182dfe37cdc15cde97cc
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::224761220970:role/github-actions-oidc-role
          aws-region: us-east-1

      - name: Verify AWS Identity
        run: aws sts get-caller-identity

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.15.8

      - name: Terraform Init
        run: terraform init -input=false

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan (Detect Drift)
        id: plan
        run: |
          set +e
          terraform plan -detailed-exitcode -out=tfplan -input=false
          echo "exitcode=$?" >> "$GITHUB_OUTPUT"
          set -e

      - name: No Drift Detected
        if: steps.plan.outputs.exitcode == '0'
        run: echo "✅ No infrastructure drift detected."

      - name: Drift Detected
        if: steps.plan.outputs.exitcode == '2'
        run: |
          echo "⚠️ Infrastructure drift detected."
          terraform show -no-color tfplan
          exit 1

      - name: Plan Failed
        if: steps.plan.outputs.exitcode == '1'
        run: |
          echo "❌ Terraform plan failed to execute."
          exit 1
<<<<<<< HEAD
    ##step that auto-generates the terraform import commands for whatever shows up as unmanaged
      - name: Install boto3
        run: pip install boto3

      - name: Export Terraform State as JSON
        run: terraform show -json > tfstate.json

      - name: Check for Unmanaged AWS Resources
        id: unmanaged_check
        continue-on-error: true
        run: python3 compare_drift.py --tf-state tfstate.json --region us-east-1 --out unmanaged.json

      - name: Generate Import Commands for Unmanaged Resources
        if: steps.unmanaged_check.outcome == 'failure'
        run: python3 generate_import_commands.py --report unmanaged.json

      - name: Upload Unmanaged Resources Report
        if: steps.unmanaged_check.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: unmanaged-resources-${{ github.run_id }}
          path: |
            tfstate.json
            unmanaged.json
            imported_resources.tf
            import_commands.sh
          retention-days: 14

      - name: Fail Job if Unmanaged Resources Found
        if: steps.unmanaged_check.outcome == 'failure'
        run: |
          echo "❌ Unmanaged AWS resources detected — see uploaded artifact for import commands."
          exit 1
=======
>>>>>>> 4560fc03478a7107d9e4182dfe37cdc15cde97cc
